jdk:
  - openjdk21
before_install:
  - sdk install java 21-tem
  - sdk use java 21-tem
  - rm -rf app xposed
  - sed -i '/include(":app")/d' settings.gradle.kts
  - sed -i '/include(":xposed")/d' settings.gradle.kts
